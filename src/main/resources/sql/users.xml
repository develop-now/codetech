<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

 <mapper namespace="users">
	<select id="getSearchList" parameterType="map" resultType="user">
		
	</select>
 		<select id="isEmail" parameterType="String" resultType="user">
 			select * from users where user_email = #{user_email}
 		</select>
 		<select id="isName" parameterType="String" resultType="user-info">
 			select * from user_info where user_name = #{user_name}
 		</select>
 </mapper>
