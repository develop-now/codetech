<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="users">
    <select id="getSearchList" parameterType="map" resultType="user">

    </select>
    <select id="isEmail" parameterType="String" resultType="user">
        select *
        from users
        where user_email = #{user_email}
    </select>
    <select id="isName" parameterType="String" resultType="user-info">
        select *
        from user_info
        where user_name = #{user_name}
    </select>
    <insert id="userInsert" parameterType="user">
    	insert into users(user_id, user_email, user_password)
    	values((select nvl(max(user_id),0)+1 from users),#{user_email},#{user_password})
    </insert>
    <select id="userId" parameterType="user" resultType="int">
    	select user_id 
    	from users
    	where user_email=#{user_email}
    </select>
    <insert id="infoInsert" parameterType="user-info">
    	insert into user_info
    	values((select nvl(max(user_id),0)+1 from user_info),#{user_name},#{user_tel},#{user_address},#{user_profile},#{original_file},#{point},#{user_id})
    	
    </insert>
    <!-- <select id="userTotalInfo" parameterType="id" resultType="upi">
    	select * 
		from (
			select user_email, user_password, user_name
			, user_tel, user_address, user_profile, point, original_file
			from USERS
			left join USER_INFO
			on USERS.USER_ID = USER_INFO.USER_ID
			where USERS.user_id = #{id}
		    )
    </select> -->
</mapper>
